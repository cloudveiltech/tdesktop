Open Telegram/build/setup.iss
Install Inno Setup from https://jrsoftware.org/isdl.php
Build the installer using Inno Setup Compiler
The resulting installer will be in the `Telegram/build/out/Release` folder

Note: Current the project file output name is `Telegram.exe`. 
Need to rename it to `CloudVeilMessenger.exe` manually before run building the installer.
Should be fixed in future.